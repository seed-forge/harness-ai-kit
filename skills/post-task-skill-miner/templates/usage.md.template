# {{loop_name}} — 使用说明

## 触发条件

本 Loop 在以下条件下被触发：

- {{trigger_condition_1}}
- {{trigger_condition_2}}

## 使用方法

### 手动触发

```bash
# 通过 loopctl 启动
loopctl run {{loop_id}} --cd <target-directory>

# 指定输入文件
loopctl run {{loop_id}} --input <input-file> --cd <target-directory>

# 指定最大迭代次数
loopctl run {{loop_id}} --max-iterations <N> --cd <target-directory>
```

### 自动触发

本 Loop 可被以下事件自动触发：

- {{auto_trigger_1}}
- {{auto_trigger_2}}

## 适用场景

- {{use_case_1}}
- {{use_case_2}}

## 不适用场景

- {{not_applicable_1}}
- {{not_applicable_2}}

## 执行流程

```
触发事件 → Maker 执行 → Checker 验证
                          ├── PASS → 输出结果 → 结束
                          └── FAIL → Maker 修复 → 重新验证
                                    └── 达到 stop_condition → 终止并升级
```

## 预期输出

- {{expected_output_1}}
- {{expected_output_2}}

## 风险级别

**{{risk_level}}** — {{risk_description}}

## 升级路径

当 Loop 无法在预算内收敛时：

1. 终止自动循环
2. 输出诊断报告（失败原因、已尝试方案、当前状态）
3. 升级到 {{escalation_target}} 处理
