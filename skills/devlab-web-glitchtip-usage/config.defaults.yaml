$schema: "harness-ai-kit-config/v1"
# 默认配置
dsn: ""                # GlitchTip DSN（必填）
environment: ""        # dev / staging / production（必填）
release: ""            # 应用版本（必填）
traces_sample_rate: 0.1
max_breadcrumbs: 50
sourcemap_upload:
  sentry_url: "https://<service-url>"
  # auth_token 通过环境变量注入，不写入配置文件
